import Link from 'next/link';

const Logo = () => {
  return (
    <Link href="/">
      <div className="text-2xl font-bold tracking-wider text-primary font-headline cursor-pointer">
        MUJER
      </div>
    </Link>
  );
};

export default Logo;
